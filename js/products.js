
/*const section_producto = document.getElementById('section-producto');

stock.array.forEach(item => {
    const product=document.createElement('div');
    if(item.categoria==="campera"){
        product.classList.add("product-card");
        product.innerHTML=`
        <img src=${item.img} class="img-card"/>
        <div class="content-card">
            <h2>${item.categoria}</h2>
            <h3>${item.marca}</h3>
            <p>$${item.precio}</p>
            <button class="button-card">Agregar al carrito</button>
        </div>
        `
        section_producto.appendChild(product);
    }
});*/


// const itemRec= stock.filter(item => item.precio<40000);
// console.log(itemRec);

//practicar foreach, find y findIndex, filter, some, if, map, set, slice, append, push, pop, fill, lenght, reduce*
